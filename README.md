# SCBD_across_scales
Data and code for reproducibility of MS 'Determinants of species contributions to beta diversity are scale dependent'

All data are provided in the 'data' folder. The source (.src) file must be run first to reproduce the code. The R scripts in the 'scripts' folder should then be run from 1-8.
